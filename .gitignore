# Secrets — never commit real env files (examples are fine)
.env
.env.*
!.env.example
mobile/.env
supabase/.env

# Local Claude settings
.claude/settings.local.json

# Dependencies (mobile has its own ignore; this covers content/ tooling)
node_modules/

# OS
.DS_Store

# Logs
*.log
