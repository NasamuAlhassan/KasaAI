# Secrets — never commit real env files (examples are fine)
.env
.env.*
!.env.example
mobile/.env
supabase/.env

# Local Claude settings
.claude/settings.local.json

# Dependencies (mobile has its own ignore; this covers content/ tooling)
node_modules/

# Supabase CLI local state (project link cache, versions — machine-specific)
supabase/.temp/

# OS
.DS_Store

# Logs
*.log
